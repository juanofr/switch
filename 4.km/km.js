let medida2 = Number(parseFloat(prompt("ingrese numero")));
switch (medida2) {
    case 1 :
        console.log("si se pasa a metros",medida2*1000);
        break;
        default:
            case 2 :
                console("si se pasa a centimetros",medida2*10000);
        break;
}